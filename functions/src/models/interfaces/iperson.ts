export interface IPerson {
  id?: string,
  mobile?: string,
  phone?: string,
  dateCreated?: Date
  dateImported?: Date,
  dateUpdated?: Date,
  email: string,
  emailStatus?: string,
  emailStatusUpdate?: Date,
  importId?: string,
  importedBy?: string,
  name: string,
  project: string,
  midName?: string,
  surname?: string,
  shortId?: string
}
